# Fan-in: encoder TP2/DP4, LLM TP4/DP2, PP=1
experiment:
  name: "fan_in_enc_tp2dp4_llm_tp4dp2"

parallelism:
  encoder: { tp: 2, dp: 4 }
  llm: { tp: 4, dp: 2 }

data:
  micro_batch_size: 2  # GBS=4 must be divisible by enc_dp=4
