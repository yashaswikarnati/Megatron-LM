# Fan-in: encoder TP2/DP4, LLM TP4/DP2
# Encoder benefits from lower TP (H=1408 is small), LLM keeps TP4 (H=4096).
experiment:
  name: "hetero_enc_tp2dp4_llm_tp4dp2"

parallelism:
  encoder: { tp: 2, dp: 4 }
  llm: { tp: 4, dp: 2 }

data:
  micro_batch_size: 2  # GBS=4 must divide enc_dp=4
