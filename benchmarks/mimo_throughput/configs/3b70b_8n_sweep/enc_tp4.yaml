# Exp 2a: Encoder TP4/DP16, 8 images * 512 seq = 4096 tokens (same vision budget)
experiment:
  name: 3b70b_enc_tp4

parallelism:
  encoder:
    tp: 4
    dp: 16

model:
  encoder:
    seq_length: 512

data:
  num_images_per_sample: 8
