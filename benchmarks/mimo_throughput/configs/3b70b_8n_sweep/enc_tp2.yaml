# Exp 2b: Encoder TP2/DP32, 16 images * 256 seq = 4096 tokens (same vision budget)
experiment:
  name: 3b70b_enc_tp2

parallelism:
  encoder:
    tp: 2
    dp: 32

model:
  encoder:
    seq_length: 256

data:
  num_images_per_sample: 16
