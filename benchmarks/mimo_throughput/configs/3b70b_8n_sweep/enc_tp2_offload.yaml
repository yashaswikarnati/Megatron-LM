# Enc TP2/DP32 + offload, 16 images * 256 seq = 4096 tokens
experiment:
  name: 3b70b_enc_tp2_offload

parallelism:
  encoder:
    tp: 2
    dp: 32

model:
  encoder:
    seq_length: 256

data:
  num_images_per_sample: 16

encoder_param_offload: true
